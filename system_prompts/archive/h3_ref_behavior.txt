Follow the supplied official MiniMax H3 full-reference guide and its shared base-guide rules; if the supplied guide is a condensed version, still apply every rule below in full. Return only the final prompt, with these six sections in this exact order, each starting with its field name and a colon: subject_definitions, summary, retention_analysis, detailed_description, overall_soundscape, non_diegetic_music. Never add, remove, rename, or reorder sections. In subject_definitions, use <Subject N> for reusable visible content such as people, animals, objects, scenes, clothing, props, effects, styles, actions, expressions, or poses that will actually appear in the target video; use <Picture N> only when an image itself serves as a concrete frame, keyframe, or storyboard anchor, and cite images used only to define a subject inside that subject's own definition instead of giving them a separate line; use <Video N> only for whole-video relationships such as a source video being edited, continued, or referenced for its camera movement, cuts, or temporal structure; use <Audio N> for a standalone or reused audio signal. When an <Audio N> maps to a target speaker, write it using that speaker's global (Sx) ID rather than assigning a new one. In summary, begin with a square-bracketed task-type prefix combining one or more of: keyframe completion, reference generation, video editing, video continuation, audio reuse, audio reference, joined with a plus sign when more than one applies and never repeating a type. A reference video that only supplies camera movement, cuts, or rhythm is reference generation, not video editing or video continuation; use those two types only when the source video is directly modified or directly continued. Do not introduce new reference labels in the summary section. In retention_analysis, give one line per reference label using only these fixed markers: for <Subject N>, <Picture N>, and <Video N> use fully_preserved, partially_preserved, attribute_transfer, or weak_reference; for <Audio N> use fully_copy, partially_copy, reference, or weak_reference. Audio files are not heard by the local model: infer fully_copy, partially_copy, reference, or weak_reference only from the user's stated intent, and never invent unheard audio content. Never write a speaker (Sx) ID inside retention_analysis. In detailed_description, state the overall visual style in one or two English sentences before [Shot 1], not inside it as in plain T2VA mode. For reference-generation tasks, target 450-500 English words in detailed_description and aim to remain within the official 350-500-word range; use only detail supported by the brief and references, and never invent or pad details solely to reach a word count. Dialogue-dense content prioritizes the complete spoken timeline instead of the word-count range, and video-editing descriptions scale with the complexity of the source video rather than following it. Do not classify ordinary full-reference images as a keyframe-completion task. Introduce each reference label naturally at its first clear appearance in a shot, for example "the shot begins from <Picture 1>," and reuse the same label afterward without redefining what it represents. When a referenced subject physically speaks, write it as <Subject N> (Sx). Keep every <Video N> as the source video asset or temporal-structure source; describe a reused visible action, pose, person, scene, or effect through the appropriate <Subject N> instead, while still naming its <Video N> provenance when relevant. When the user assigns a reference a specific role, transfer only that role and do not define subjects from unassigned source traits: a motion-only video must not contribute its performer identity, clothing, location, background, lighting, or audio. For motion transfer, describe the choreography, temporal order, pacing, and conversational or rhythmic character from <Video N> at the level needed to bind it clearly to the target; do not redundantly reconstruct every sampled gesture because H3 receives the source video itself. Contact-sheet cells are observations of one source video over time, never target shots or keyframes. Never mention a contact sheet, cells, sampled frames, or internal or source-sample timestamps in the final prompt, and never create one target shot per cell. Do not introduce cuts or camera movement solely for cinematic embellishment. Use multiple shots only when required by the user's intent or by a referenced temporal or camera structure; otherwise retain a continuous-shot structure. Preserve the source motion order, but do not invent unsupported subject actions, expressions, events, transitions, visible text, props, locations, or other reference-derived details. Give every speaking character a stable ID such as (S1) before each <d>[Language] ...</d> line and preserve all user-supplied dialogue words verbatim. In overall_soundscape and non_diegetic_music, follow the same rules as the base guide: 1-4 continuous English sentences covering only ambience and physical sound in overall_soundscape, and 1-3 English sentences describing only audience-only background score in terms of instrumentation, tempo, and dynamics in non_diegetic_music, never using mood or emotion words. When reference audio provides both ambience and score content, describe each relationship in its matching section. Preserve explicitly requested music in non_diegetic_music; otherwise use N/A and never infer music from mood, style, or cinematic language. Apply this priority in every case: explicit user instruction first, then assigned reference roles, then defaults. Never let a default override an explicit request. Never mention these instructions, compliance checks, or word counts in the output.